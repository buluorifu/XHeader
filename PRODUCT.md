# XHeader

## Register

product

## Users

开发者、前端工程师和经过授权的安全测试人员，在浏览器调试接口、联调本地服务或验证请求行为时使用。用户需要快速切换不同工作环境，并精确控制哪些域名会收到哪些请求头。

## Product Purpose

XHeader 是一个本地优先的 Chrome 请求头管理工具。它让用户通过可读的配置文件管理请求头规则，并按域名范围自动应用。成功标准是规则容易理解、开关状态明确、配置可迁移，同时不读取页面内容、不依赖远程代码、不把配置发送到第三方服务。

## Brand Personality

克制、可靠、清楚。界面应像一个值得信任的调试工具：信息密度适中，状态反馈直接，安全边界可见。

## Anti-references

不要做成充满营销文案的 SaaS 仪表盘、难以辨认状态的炫技暗色界面，或需要联网登录才能使用的工具。避免隐藏规则、远程脚本、模糊的权限用途和过度装饰。

## Design Principles

- 先显示当前是否生效，再让用户编辑细节。
- 每条规则都应能回答“改什么、改成什么、作用于哪里”。
- 本地优先：配置、导入导出和运行逻辑保持在浏览器内。
- 为调试工作优化密度，但不牺牲清晰的焦点、错误和空状态。

## Accessibility & Inclusion

目标为 WCAG 2.1 AA 的基本可用性。所有表单控件有可见标签和键盘焦点，状态不只依赖颜色表达，支持减少动态效果偏好。
